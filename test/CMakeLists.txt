CMAKE_MINIMUM_REQUIRED (VERSION 2.8)

PROJECT (shmap_test)

INCLUDE_DIRECTORIES (..)

LINK_DIRECTORIES (..)
LINK_LIBRARIES (shmap pthread rt dl)

ADD_COMPILE_OPTIONS (-Wall -g --std=c99)

ADD_DEFINITIONS (-D_SVID_SOURCE)

SET (SRCS test.c)

ADD_EXECUTABLE (test ${SRCS})
